// this type of operator are not for practical use
// so discussed separately
// in decimal
// 1 is represented as 1 = 00000001
// 2 is 00000010
console.log(1 | 2); // add their decimal represented bits, if at least one found, then answer is one
console.log(1 & 2); // will be one if all are one
