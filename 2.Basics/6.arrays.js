let someColors = ["white", "black"];

//we can assign different data type into the array
someColors[2] = 1;

console.log(someColors);

// every arrays are object
//they have properties and functions just all refernece types
