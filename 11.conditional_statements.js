// 2 types conditional statements
// if.....else(if - else if - else if -else)
// switch...case

// only switch case done because not familiar with it
let role = "Admin";
switch (role) {
  case "Admin":
    console.log("The Admin");
    break;

  default:
    console.log("Guest");
}
